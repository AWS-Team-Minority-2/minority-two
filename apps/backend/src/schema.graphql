type Query {
  getUsers: [UserOutput]!
}

type Mutation {
  addUser(flag: User): AddedUserResponse
}

type AddedUserResponse {
  message: String
}

type UserOutput {
  id: String!
  name: String!
}

input User {
  id: String!
  name: String!
}
